# Карта курса

Курс состоит из 151 микроурока, 585 заданий и 32 экзаменов. Перед основным материалом идёт «Спокойный старт» из 20 уроков: в каждом появляется одна новая идея, затем исполняемый пример, прогноз, практика и повторение. Каждый раздел заканчивается смешанной контрольной точкой с обязательным практическим заданием; код в ней проверяется на дополнительном незнакомом сценарии, а не на совпадении с авторским синтаксисом.

| № | Раздел | Что освоит ученик |
| ---: | --- | --- |
| 0 | Спокойный старт | Как учиться без перегрузки: `print`, строки, переменные, типы, `len`, f-строки, `input`, `int`, `lower`, `strip`, ошибки и повторение |
| 1 | Старт: говорим с Python | Первый вывод, переменные, типы, ввод |
| 2 | Логика и повторения | `if`, `for`, `while` |
| 3 | Функции и данные | Функции, списки, словари, множества |
| 4 | Python в деле | Файлы, ошибки, первые классы |
| 5 | Операторы и выражения | Арифметика, boolean-логика, точность чисел |
| 6 | Строки глубже | Срезы, поиск, `split`, `join`, f-строки |
| 7 | Кортежи и последовательности | Распаковка, срезы, `zip`, `enumerate` |
| 8 | Списки в работе | Сортировка, стек, очередь, мутации |
| 9 | Словари и множества | `get`, `items`, объединение, операции множеств |
| 10 | Продвинутая логика | `elif`, `match`, `break`, `continue`, loop `else` |
| 11 | Функции: следующий уровень | Defaults, keyword args, `*args`, `**kwargs` |
| 12 | Области видимости | LEGB, замыкания, `lambda`, ловушка mutable default |
| 13 | Генерация коллекций | List/dict/set comprehensions |
| 14 | Итераторы и генераторы | `iter`, `next`, `yield`, generator expressions |
| 15 | Модули и пакеты | Импорт, `__main__`, структура пакета |
| 16 | Файлы и форматы данных | `pathlib`, текст, JSON, CSV |
| 17 | Ошибки и отладка | Иерархия ошибок, `raise`, `finally`, traceback |
| 18 | ООП: проектирование | `__init__`, `__repr__`, композиция |
| 19 | ООП: расширение | Наследование, свойства, протоколы, dunder-методы |
| 20 | Типы и модели данных | Аннотации, union, `dataclass`, `Enum` |
| 21 | Стандартная библиотека I | `datetime`, `random`, `statistics`, `Decimal` |
| 22 | Стандартная библиотека II | `collections`, `itertools`, `functools`, `heapq` |
| 23 | Регулярные выражения | Паттерны, группы, валидация, замена |
| 24 | Тестирование | Unit-тесты, фикстуры, параметризация, mocks |
| 25 | Качество и Git | PEP 8, Ruff, docstrings, история изменений |
| 26 | CLI и окружение | `argparse`, env, venv, `pyproject.toml` |
| 27 | HTTP и API | Запросы, JSON, статусы, пагинация |
| 28 | Базы данных | Реляционная модель, CRUD, параметры, транзакции |
| 29 | Параллельность | Потоки, процессы, блокировки, очереди |
| 30 | Асинхронный Python | Coroutines, tasks, `gather`, timeouts |
| 31 | Выпускные проекты | Менеджер задач, заметки, API-клиент, релиз |

## Принципы сложности

- Сначала один новый концепт за урок, затем — его комбинация с уже знакомыми. Функции идут шагами: вызов → параметр → `return` → сборка → именованный аргумент.
- Условия, циклы и коллекции также разделены: `if` → `else`, `for` → накопитель, `while` → `break`, список → `append` → `split` → `sum`, словарь → множество.
- Curriculum-linter проверяет теорию, варианты, заготовки, подсказки и скрытые тесты: синтаксис, builtin или метод не может появиться до вводящего урока.
- Перед ответом ученик видит нейтральный план; подсказки открываются по одной, а почти готовая форма — только после своей попытки.
- Каждое задание явно связано с нужными уже пройденными инструментами. Сворачиваемый справочник напоминает точное имя (`.lower()`, `.append()`, `len()`), форму вызова, назначение и отдельный нейтральный пример; для накопительного кода он описывает повторяемую старую тему, а не новую тему текущего урока.
- Начальный блок повторяет базовые действия в разных контекстах, чтобы сформировать память до перехода к условиям и циклам.
- Поведенческие тесты принимают эквивалентные решения; структурная AST-проверка используется только когда форма прямо названа учебной целью.
- Урок требует 75% и обязательный самостоятельный код. Контрольная точка не открывается до завершения раздела, не сдаётся одними вариантами ответа и открывает следующий раздел.
- Новые навыки возвращаются в смешанной практике по интервалам 1/3/7/14/30; слабое место закрывается после двух верных самостоятельных повторений подряд.
- Проекты в конце повторяют все ключевые слои: данные, логику, I/O, API, тесты и документацию.

## Мастерская проектов

Параллельно с курсом открываются 15 практических инструментов. Каждый проект требует предыдущий проект и конкретные lesson ID, поэтому не прыгает в незнакомые темы:

1. Именная открытка — `input()`, переменная и f-строка.
2. Калькулятор покупки — преобразование `int()` и арифметика.
3. Проверка доступа — условие `if` / `else`.
4. Счётчик слов — `split()`, список и `len()`.
5. Сводка оценок — список, `sum()` и среднее.
6. Карточка контакта — словарь и красивый вывод данных.
7. JSON-хранилище заметок — запись и чтение в виртуальном файле.
8. Кошелёк расходов — класс, методы и состояние.
9. Лаборатория тестов — `assert`, нормализация и новые кейсы.
10. Маршрутизатор CLI — чистая обработка аргументов.
11. Разбор ответа API — JSON-словарь и устойчивая сводка.
12. Безопасный SQLite-запрос — SQL-шаблон и параметры отдельно.
13. Планировщик очереди — `queue.Queue` и распределение работ.
14. Асинхронная партия — корутины и `asyncio.gather()`.
15. Финальный менеджер задач — ООП, коллекции, циклы и JSON.

У каждого проекта есть стартовый код, контрольный список, справочник нужных функций и методов, постепенно раскрываемые подсказки, безопасный запуск с консольным выводом и несколько контрольных сценариев. Захардкоженный ответ под один пример проверку не проходит.

Подробные обязательные правила авторинга, повторения, экзаменов и проверки эквивалентных решений находятся в [LEARNING_DESIGN.md](LEARNING_DESIGN.md).
